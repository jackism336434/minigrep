# minigrep 完善路线

> 目标：在保留教学项目简洁性的基础上，逐步将 `minigrep` 完善为一个可用于日常文本搜索的命令行工具。
>
> 建议按阶段推进，每完成一项同步补充测试和文档。

## 当前基线

- [x] 支持单个 UTF-8 文件搜索
- [x] 支持大小写敏感搜索
- [x] 支持通过 `IGNORE_CASE` 环境变量进行大小写不敏感搜索
- [x] 将核心搜索逻辑拆分到 `lib.rs`
- [x] 已为核心搜索逻辑编写单元测试
- [x] 已编写基础 README

## 第一阶段：完善基础 CLI

目标：让命令行行为清晰、稳定，具备常用搜索选项。

- [ ] 设计统一的命令行参数结构
  - [ ] 支持 `--help` 和 `--version`
  - [ ] 参数缺失时显示清晰的用法说明
  - [ ] 拒绝并提示未知参数
  - [ ] 拒绝并提示多余参数
- [ ] 支持 `-i` / `--ignore-case`
  - [ ] 保留 `IGNORE_CASE` 作为兼容方式，明确优先级
- [ ] 支持 `-n` / `--line-number`，输出匹配行号
- [ ] 支持 `-v` / `--invert-match`，输出不匹配的行
- [ ] 支持 `-c` / `--count`，只输出匹配数量
- [ ] 支持 `-q` / `--quiet`，只通过退出码表示结果
- [ ] 增加空查询、空文件和无匹配结果的行为说明
- [ ] 改进错误输出
  - [ ] 错误信息输出到 stderr
  - [ ] 包含具体文件路径和失败原因
- [ ] 增加命令行行为测试

## 第二阶段：规范退出码和错误处理

目标：让工具可以可靠地用于 Shell 脚本和自动化任务。

- [ ] 约定并实现退出码
  - [ ] `0`：找到至少一个匹配
  - [ ] `1`：搜索成功但没有匹配
  - [ ] `2`：参数、文件或其他运行错误
- [ ] 区分参数错误、文件读取错误和搜索结果
- [ ] 处理输出管道提前关闭的情况（例如通过 `head` 截断输出）
- [ ] 为错误路径补充测试

## 第三阶段：支持 stdin 和多个文件

目标：支持常见的 Unix 管道和批量搜索场景。

- [ ] 支持从标准输入读取内容
- [ ] 使用 `-` 表示 stdin
- [ ] 支持一次传入多个文件
- [ ] 搜索多个文件时显示文件名
- [ ] 提供隐藏或强制显示文件名的选项（`-h` / `-H`）
- [ ] 处理部分文件读取失败的情况
- [ ] 为 stdin、多个文件和文件名输出补充测试

## 第四阶段：流式读取和性能优化

目标：降低大文件搜索的内存占用，并减少不必要的分配。

- [ ] 使用 `BufReader` 逐行读取文件
- [ ] 避免通过 `read_to_string` 一次性加载整个文件
- [ ] 评估大小写不敏感搜索中的逐行 `to_lowercase` 分配
- [ ] 统一处理读取错误和行读取错误
- [ ] 增加大文件基准测试
- [ ] 对比优化前后的内存占用和执行时间
- [ ] 在性能优化前后保持搜索结果一致

## 第五阶段：正则表达式和匹配模式

目标：从简单的子串搜索扩展为更强的文本匹配工具。

- [ ] 引入正则表达式搜索能力
- [ ] 支持正则表达式大小写不敏感模式
- [ ] 支持固定字符串模式（类似 `grep -F`）
- [ ] 支持整词匹配（类似 `-w`）
- [ ] 支持整行匹配（类似 `-x`）
- [ ] 为无效正则表达式提供明确错误信息
- [ ] 增加正则、转义字符和 Unicode 场景测试

## 第六阶段：输出增强

目标：提升日志分析和交互使用体验。

- [ ] 支持 `-l` / `--files-with-matches`，只输出匹配文件名
- [ ] 支持 `-o` / `--only-matching`，只输出匹配文本
- [ ] 支持 `-A`，显示匹配行之后的上下文
- [ ] 支持 `-B`，显示匹配行之前的上下文
- [ ] 支持 `-C`，显示匹配行前后的上下文
- [ ] 为不同匹配块添加分隔符
- [ ] 支持匹配内容颜色高亮
- [ ] 在重定向到文件或管道时自动关闭颜色
- [ ] 增加输出格式测试

## 第七阶段：目录递归和文件筛选

目标：支持项目目录和日志目录中的批量搜索。

- [ ] 支持目录输入
- [ ] 支持递归搜索（类似 `-r`）
- [ ] 明确符号链接的处理策略
- [ ] 支持按扩展名或 glob 筛选文件
- [ ] 提供跳过隐藏文件的选项
- [ ] 提供跳过二进制文件的选项
- [ ] 评估是否支持 `.gitignore`
- [ ] 避免递归遍历导致无限循环
- [ ] 为权限错误、空目录和嵌套目录补充测试

## 第八阶段：非 UTF-8 和二进制文件处理

目标：提高对真实文件和日志数据的兼容性。

- [ ] 评估从 UTF-8 字符串搜索改为字节搜索的可行性
- [ ] 检测并提示二进制文件
- [ ] 提供强制文本搜索选项（类似 `--text`）
- [ ] 明确无效 UTF-8 数据的处理方式
- [ ] 明确不同平台换行符的处理行为
- [ ] 增加二进制文件和非法 UTF-8 测试

## 第九阶段：工程质量

目标：让项目更容易维护、发布和验证。

- [ ] 将 `Config`、参数解析和 `run` 拆分为易测试的模块
- [ ] 增加 CLI 集成测试
- [ ] 增加端到端测试脚本
- [ ] 使用 `cargo fmt` 统一代码格式
- [ ] 使用 `cargo clippy` 修复可改进项
- [ ] 配置 CI，自动运行格式检查、Clippy 和测试
- [ ] 增加发布构建验证：`cargo build --release`
- [ ] 补充 crate 文档和公共函数注释
- [ ] 增加项目许可证
- [ ] 更新 README 中的参数、示例和限制说明

## 推荐实施顺序

如果希望控制项目规模，建议优先完成以下内容：

1. 基础 CLI 参数：`-i`、`-n`、`-v`、`-c`、`--help`
2. 正确的退出码和 stderr 错误输出
3. stdin 和多个文件支持
4. `BufReader` 流式读取
5. 正则表达式搜索
6. 上下文输出和颜色高亮
7. 目录递归搜索
8. 二进制文件、性能和 CI 优化

## 每个功能完成时的检查清单

- [ ] 明确命令行行为和边界情况
- [ ] 编写或更新单元测试
- [ ] 编写 CLI/集成测试
- [ ] 运行 `cargo fmt --check`
- [ ] 运行 `cargo clippy -- -D warnings`
- [ ] 运行 `cargo test`
- [ ] 更新 README 使用示例
